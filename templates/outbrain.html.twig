<div class="outbrain_div_container">
    <amp-embed
        width="100" height="100"
        type="outbrain"
        layout="responsive"
        data-widgetIds="AMP_1"
        data-htmlURL="{{ canonical_url }}"
        data-ampURL="{{ amp_url }}"
    >
    </amp-embed>
</div>
